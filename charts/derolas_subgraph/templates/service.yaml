apiVersion: v1
kind: Service
metadata:
  name: {{ include "derolas_subgraph.fullname" . }}
  labels:
    {{- include "derolas_subgraph.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "derolas_subgraph.selectorLabels" . | nindent 4 }}
