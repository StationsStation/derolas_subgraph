apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "derolas_subgraph.fullname" . }}-test-connection"
  labels:
    {{- include "derolas_subgraph.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "derolas_subgraph.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
